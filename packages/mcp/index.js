// Export MCP client and server components
const { PrismaMCPClient } = require('./client');

module.exports = {
  PrismaMCPClient,
  // Server is typically run as a standalone process via server.js
};
